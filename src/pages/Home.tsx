import ProgressTracker from "../components/ProgressTracker";

const Home = () => {
    return (
        <div className="min-h-screen bg-gray-50">
            <ProgressTracker />
        </div>
    );
};

export default Home;